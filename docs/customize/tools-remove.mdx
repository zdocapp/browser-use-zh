---
title: "Remove Tools"
description: "Exclude default tools from your controller"
icon: "minus"
mode: "wide"
---

You can exclude default tools when creating a controller:

```python
from browser_use import Controller

controller = Controller(exclude_actions=['search_google', 'wait'])
agent = Agent(task='...', llm=llm, controller=controller)
```

